{{--
<div class="border rounded shadow-sm p-3">
    <div class="d-flex justify-content-between">
        <div class="mb-1">
            <a href="{{route('drug.index', ['category'=>$drug->category->id])}}" class="link-dark fw-bold text-decoration-none">
                {{$drug->category->name}}
            </a>
        </div>
        <div class="mb-1">
            <a href="{{route('drug.index', ['hard_drug'=>$drug->hard_drug->id])}}" class="link-dark fw-bold text-decoration-none">
                {{$drug->hard_drug->name}}
            </a>
        </div>
        <div class="mb-1">
            <a href="{{route('drug.index', ['soft_drug'=>$drug->soft_drug->id])}}" class="link-dark fw-bold text-decoration-none">
                {{$drug->soft_drug->name}}
            </a>
        </div>
        <div class="mb-1">
            <a href="{{route('drug.index', ['liquid'=>$drug->liquid->id])}}" class="link-dark fw-bold text-decoration-none">
                {{$drug->liquid->name}}
            </a>
        </div>
    </div>
</div>
--}}

